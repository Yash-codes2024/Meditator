<!DOCTYPE html>
<html>
<head>
    <title>Meditate</title>
    <style>
        .container {
            text-align: center;
            border: 2px solid rgb(195, 0, 255);
            background-image: linear-gradient(#2CC9B8, #C1DCEC, #84A3BF  );
            width: 100%;
            height: 100%;
            
        }
        button {
            padding: 15px 30px;
            font-size: 18px;
            margin: 10px;
            cursor: pointer;
            border-radius: 20px;
            background-image: linear-gradient(#94C2DF, #84A3BF, #72B5EC );

        }

        #videoFeed {
            border: 2px solid #333;
            max-width: 800px;
            margin: 20px auto;
            border-radius: 20px;
        }
        
    </style>
</head>
<body>
    <div class="container">
        <h1>Let's Meditate</h1>
        <img id="videoFeed" src="{{ url_for('video_feed') }}">
        <div>
            <button onclick="startCamera()">Start Camera</button>
            <button onclick="stopCamera()">Stop Camera</button>
        </div>
    </div>

    <script>
        function startCamera() {
            fetch('/start')
                .then(response => console.log('Camera started'))
                .catch(error => console.error('Error:', error));
        }

        function stopCamera() {
            fetch('/stop')
                .then(response => console.log('Camera stopped'))
                .catch(error => console.error('Error:', error));
        }
    </script>
    
</body>
</html>

<!---*
        {
            background-image: linear-gradient(to right, rgb(183, 59, 17) , rgb(190, 184, 24));

        }
--->
